#include <stdio.h>#define N 10//冒泡法：每次比较前后两个数的大小，一共N-1次int main(){    int a[N] = { 0 };    int i, j, t;    printf("Input 10 numbers:\n");    for (i = 0; i < N; i++)    {        scanf_s("%d", &a[i]);        printf("\n");    }            for (j = 0; j < N - 1; j++)     {        for (i = 0; i < N - 1 - j; i++)         {            if (a[i] > a[i + 1])            {                t = a[i];//交换数值的中间变量                a[i] = a[i + 1];                a[i + 1] = t;            }        }                }            printf("The sorted numbers:\n");    for (i = 0; i < N; i++)     {        printf("%d ", a[i]);    }        }